# Example ParallelCluster 3.x config using aws-parallelcluster-monitoring.
# Tested on alinux2, alinux2023, ubuntu22.04, ubuntu24.04, rhel9.
#
# Pin the monitoring stack by git tag (first arg to post-install.sh).
# Bump the tag here when you upgrade.
HeadNode:
  InstanceType: c5.xlarge
  Networking:
    SubnetId: subnet-123456
    AdditionalSecurityGroups:
      - sg-123456
  Ssh:
    KeyName: keypair
  LocalStorage:
    RootVolume:
      VolumeType: gp3
  CustomActions:
    OnNodeConfigured:
      Script: https://raw.githubusercontent.com/aws-samples/aws-parallelcluster-monitoring/main/post-install.sh
      Args:
        - v1.0
  Iam:
    AdditionalIamPolicies:
      - Policy: arn:aws:iam::aws:policy/AmazonSSMManagedInstanceCore
      - Policy: arn:aws:iam::aws:policy/CloudWatchFullAccess
      - Policy: arn:aws:iam::aws:policy/AWSPriceListServiceFullAccess
      - Policy: arn:aws:iam::aws:policy/AmazonSSMFullAccess
      - Policy: arn:aws:iam::aws:policy/AWSCloudFormationReadOnlyAccess
Scheduling:
  Scheduler: slurm
  SlurmQueues:
    - Name: queue0
      AllocationStrategy: lowest-price
      ComputeResources:
        - Name: queue0-compute-resource-0
          Instances:
            - InstanceType: c5n.large
          MinCount: 0
          MaxCount: 4
      Networking:
        SubnetIds:
          - subnet-123456
      ComputeSettings:
        LocalStorage:
          RootVolume:
            VolumeType: gp3
      CustomActions:
        OnNodeConfigured:
          Script: https://raw.githubusercontent.com/aws-samples/aws-parallelcluster-monitoring/main/post-install.sh
          Args:
            - v1.0
      Iam:
        AdditionalIamPolicies:
          - Policy: arn:aws:iam::aws:policy/AmazonSSMManagedInstanceCore
          - Policy: arn:aws:iam::aws:policy/CloudWatchFullAccess
          - Policy: arn:aws:iam::aws:policy/AWSPriceListServiceFullAccess
          - Policy: arn:aws:iam::aws:policy/AmazonSSMFullAccess
          - Policy: arn:aws:iam::aws:policy/AWSCloudFormationReadOnlyAccess
Region: us-east-2
Image:
  # alinux2023 is the PC 3.8+ default. alinux2, ubuntu22/24, rhel9 also work.
  Os: alinux2023
Tags:
  - Key: 'Grafana'
    Value: 'true'
